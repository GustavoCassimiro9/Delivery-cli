### Como criar o bd
rode no terminal: `docker composer up -d`

### Rodar a cli
Entre no diretorio delivery_cli: `cd ./delivery_cli`
Baixe as dependencias do requirements.txt: `pip install -r requirements.txt`
Inicie a aplicacao: `python app.py`
